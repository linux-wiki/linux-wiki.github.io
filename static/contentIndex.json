{"index":{"slug":"index","filePath":"index.md","title":"index","links":["linux-kernel"],"tags":[],"content":"Linux开发文档\nLinux内核文档\nlinux-kernel"},"linux-kernel/RCU":{"slug":"linux-kernel/RCU","filePath":"linux-kernel/RCU.md","title":"RCU","links":[],"tags":[],"content":"RCU是什么\nRCU（Read-Copy-Update）是一种面向读多写少场景的并发同步机制：\n\n读操作几乎无锁，写操作通过“复制 + 延迟回收”保证一致性\n\n它的核心思想不是“阻止冲突”，而是：\n\n允许读线程并发读旧数据\n写线程生成新版本\n旧版本延迟释放，等所有读者结束后再回收\n\n⚠️ 边界很重要\nRCU 解决的是：\n\n读多写少的数据一致性问题\n\nRCU 不适合：\n\n强一致写写互斥\n需要实时看到最新值的逻辑\n\nRCU 解决什么问题\n在没有 RCU 之前，内核常见同步方式：\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n方法问题spinlock读也被阻塞，性能差rwlock读可以并发，但写仍然阻塞读mutex太重\n典型痛点场景\n例如：\n\ndentry 缓存\ninode 查找\n路由表\nNetfilter hook\ntask struct 访问\n\n这些特点是：\n\n读远多于写（99% 读）\n读路径必须极快\n不能加锁影响性能\n\n👉 所以 RCU 出现：\n\n把“同步问题”变成“版本问题”\n\nRCU 的核心机制\nRCU 由三部分组成：\n\n1. 读侧（RCU Reader）\n特点：\n\n无锁\n通过 rcu_read_lock() 标记临界区\n访问数据时用 rcu_dereference()\n\n核心规则：\n\n读者不会阻塞写者，也不会被写者阻塞\n\n示例逻辑：\nrcu_read_lock();p = rcu_dereference(global_ptr);use(p);rcu_read_unlock();\n\n👉 读者看到的是：\n\n当前版本 or 旧版本（但一定合法）\n\n2. 写侧（RCU Writer）\n写操作不是“修改”，而是：\n\n❗复制 + 替换指针\n\n流程：\n\n复制旧对象\n修改新对象\n用 rcu_assign_pointer() 原子替换\n等待旧读者结束\n释放旧对象\n\n3. 回收机制（Grace Period）\n关键点：\n\n旧数据不能立即释放\n\n必须等待：\n\n所有 CPU 离开 RCU 读临界区\n\n这个等待周期叫：\nGrace Period\nRCU 使用场景（非常重要）\n✔ 文件系统\n\ndentry cache\ninode lookup\n\n例如 dentry-&gt;d_fsdata：\n👉 非常典型 RCU 使用点\n\n✔ Netfilter\nhook 链表遍历：\n\n读：遍历规则链\n写：更新规则表\n\n\n✔ 路由表\n\n路由查找\nFIB 表访问\n\n\n✔ 进程管理\ntask_struct 访问（部分路径）\nRCU vs 其他机制（对比层）\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n机制读性能写性能一致性适用spinlock差中强小临界区rwlock中差强读多写少RCU极强中/差最终一致超高读场景\n关键本质区别：\nspinlock/rwlock：\n\n“阻止冲突”\n\nRCU：\n\n“允许冲突 + 版本隔离”\n\nRCU例子\n读路径（安全访问）\nrcu_read_lock();\n \nstruct my_data *d;\nd = rcu_dereference(global_data);\n \nif (d) {\n    do_something(d);\n}\nrcu_read_unlock();\n写路径（替换）\nstruct my_data *new = kmalloc(...);\nstruct my_data *old;\n \nold = xchg(&amp;global_data, new);\n \ncall_rcu(&amp;old-&gt;rcu, free_callback);\n回收（关键）\nvoid free_callback(struct rcu_head *rh)\n{\n    kfree(container_of(rh, struct my_data, rcu));\n}\nRCU 容易踩的坑（重点）\n1. 没有 rcu_dereference\n直接读：\np = global_ptr;\n👉 可能编译器重排序 / CPU乱序\n2. 在RCU区域sleep\nrcu_read_lock();\nschedule(); // ❌ 错\n3. 写完立即free\nRCU不允许\nkfree(old);\n必须：\ncall_rcu(...)\n❌ 4. 用 RCU 做强一致\nRCU 不是事务系统！\n进阶理解（机制本质）\nRCU 的本质其实是：\n\n延迟回收 + 读无锁 + 写版本替换\n\n可以抽象成：\n旧版本 ----------\\ \n              -&gt; 并发读\n新版本 ----------/\n\n等“所有读者结束”后：\n释放旧版本\n\n用一句话总结\nRCU 本质就是：\n\n用“时间换空间一致性”，用“延迟回收”换“读路径零锁”\n\n最核心的 RCU 三件套\nRCU 最重要的只有三个 API\nrcu_read_lock();\nrcu_dereference();\nrcu_read_unlock();\n和：\nrcu_assign_pointer();\ncall_rcu();\n完整代码例子\n1. 数据结构\n#include &lt;linux/rcupdate.h&gt;\n#include &lt;linux/slab.h&gt;\n#include &lt;linux/dcache.h&gt;\n \nstruct safe_info {\n    int                     encrypt;\n    int                     version;\n \n    struct rcu_head         rcu; // 必须，因为 call_rcu() 最终靠它回调释放\n};\n2. 读路径（最重要）\n正确写法：\nstatic struct safe_info*\nsafe_get_info_rcu(struct dentry* dentry)\n{\n    struct safe_info* info;\n \n    rcu_read_lock();\n \n    info = rcu_dereference(dentry-&gt;d_fsdata);\n \n    if (info) {\n        printk(&quot;encrypt=%d version=%d\\n&quot;,\n               info-&gt;encrypt,\n               info-&gt;version);\n    }\n \n    rcu_read_unlock();\n \n    return info;\n}\n为什么必须 rcu_dereference\n因为：\ninfo = dentry-&gt;d_fsdata;\n不安全。\nCPU 可能：\n\n乱序\nspeculative load\ncache reorder\n\n导致：\n指针已经可见但对象字段还没初始化完成\n\n于是：\ninfo != NULL但 info-&gt;encrypt 是垃圾\n\n所以：\nrcu_assign_pointer()+rcu_dereference()\n\n是一对。\n\n3. 写路径（更新）\n错误写法\nold = dentry-&gt;d_fsdata;new = kmalloc(...);dentry-&gt;d_fsdata = new;kfree(old);\n\n这会直接炸。\n因为 reader 可能还在访问 old。\n\n正确写法\nstatic int\nsafe_update_info(struct dentry* dentry, int encrypt, int version)\n{\n    struct safe_info* old;\n    struct safe_info* new;\n \n    new = kzalloc(sizeof(*new), GFP_KERNEL);\n    if (!new)\n        return -ENOMEM;\n \n    new-&gt;encrypt = encrypt;\n    new-&gt;version = version;\n \n    old = rcu_dereference_protected(\n                dentry-&gt;d_fsdata,\n                1);\n \n    /*\n     * 发布新对象\n     */\n    rcu_assign_pointer(dentry-&gt;d_fsdata, new);\n \n    /*\n     * 延迟释放旧对象\n     */\n    if (old)\n        call_rcu(&amp;old-&gt;rcu, safe_info_rcu_free);\n \n    return 0;\n}\n4. RCU 回调释放\nstatic void\nsafe_info_rcu_free(struct rcu_head* rcu)\n{\n    struct safe_info* info;\n \n    info = container_of(rcu,\n                        struct safe_info,\n                        rcu);\n \n    printk(&quot;free old info\\n&quot;);\n \n    kfree(info);\n}\n5. 删除路径\n很多人：\nkfree(dentry-&gt;d_fsdata);\ndentry-&gt;d_fsdata = NULL;\n直接炸。\n\n正确删除\nstatic void safe_destroy_info(struct dentry* dentry)\n{\n    struct safe_info* old;\n \n    old = rcu_dereference_protected(\n                dentry-&gt;d_fsdata,\n                1);\n \n    RCU_INIT_POINTER(dentry-&gt;d_fsdata, NULL);\n \n    if (old)\n        call_rcu(&amp;old-&gt;rcu,\n                 safe_info_rcu_free);\n}"},"linux-kernel/index":{"slug":"linux-kernel/index","filePath":"linux-kernel/index.md","title":"index","links":["linux-kernel/RCU"],"tags":[],"content":"基础概念\nRCU"}}